/ Delete All Builds  for a Given Job Name/

def jobName = "Your Job Name"; // change this variable to match the name of the job whose builds you want to delete
def job = Jenkins.instance.getItem(jobName)
job.getBuilds().each { it.delete() }
job.nextBuildNumber = 1
job.save()
System.setProperty("hudson.util.ProcessTree.disable", "true");
